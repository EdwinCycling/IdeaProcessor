rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Only allow authenticated users (Admins) to read/write storage
      allow read, write: if request.auth != null;
    }
  }
}
